query Tasks {
  tasks(first: 5) {
    edges {
      cursor
        node {
          id
          type
          description
          logs {
            id
          }
        }
    }
    pageInfo {
      hasNextPage
    }
  }
}
